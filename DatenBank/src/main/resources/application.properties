server.port=8081

#if there are problems with the database(e.g. because of different database model), change this from update
#to create, start database, end it and then change it to update again for a "restart" of the database.
#if problems with database still persist, delete and create the mysql database again in mysql
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/datenbank
spring.datasource.username=root
spring.datasource.password=AgileQuads123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.jpa.show-sql: true


